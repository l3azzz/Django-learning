+-------------------------------------------------------------------+
|                                                                   |
|                   An Introduction to Django                       |
|                                                                   |
+-------------------------------------------------------------------+

### 1. Create the Main Project Folder
mkdir django_1
cd django_1

### 2. Set Up the Virtual Environment
python3 -m venv venv

# Activate the environment (use the command for your OS)
# On Windows:
venv\Scripts\activate.bat
# On macOS/Linux:
source venv/bin/activate

----make directory src----
# mkdir src

### 3. Install Django --- go to main dir
pip install django

# You can verify the correct Python is being used
# On Windows: where python
# On macOS/Linux: which python

### 4. Create the Source Directory  - in Django in django
# This is where your project's code will live.
mkdir src
cd src

### 5. Start the Django Project
# Use the project name and a '.' to create it in the current (src) directory.
django-admin startproject django_1 .

### 6. Run the Development Server
# Make sure you are in the 'src' directory where manage.py is located.
python manage.py runserver

# Open your browser and go to http://127.0.0.1:8000/ to see your site.
# The server will automatically refresh when you save your files.


---------------------------------------------------------------------
# Create project directory and navigate into it
mkdir django_1
cd django_1

# Create a virtual environment
python3 -m venv venv

# Activate the environment (use the command for your OS)
# On Windows:
venv\Scripts\activate.bat
# On macOS/Linux:
source venv/bin/activate

# Install Django
pip install django

# Create a source directory and navigate into it
mkdir src
cd src

# Start the Django project in the current directory
django-admin startproject config .

# Run the development server
python manage.py runserver
---------------------------------------------------------------------









































human version


Intro to dJango

u know python is better than js 
To use python on backend, we can use django framework
go to current location to install the jango folder 
make a directory for the file with name --- mkdir django_1
then go to that directory we made --------- cd django_1
create a envirornment there so blah ------- python3 -m venv venv
now activate that thing to run     -------- venv\Scripts\activate.bat
create a src file in the folder   --------- mkdir src

inside src the entire code lies



install and run

first check which python it uses -- which python 
go to maing dir django_1         -- cd django_1
install djanog                   -- pip install django
now go to src folder --empty     -- cd src
now start the project ------------- django-admin startproject {current main folder name --django_1}
to run go to manage.py location  -- cd django_1 -- python manage.py runserver -thengo to the location using localhostblalh